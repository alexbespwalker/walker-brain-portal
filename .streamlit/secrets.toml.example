# Copy this to secrets.toml and fill in actual values.
# NEVER commit secrets.toml to version control.
#
# For Streamlit Cloud: paste these values into the app's
# "Advanced settings" > "Secrets" field during deployment.

[auth]
password = "CHANGE_ME"
admin_password = "CHANGE_ME"

[database]
url = "https://beviondsojrrdvknpdbh.supabase.co"
# Use the ANON (public) key, NOT the service role key.
# RLS is disabled on Walker Brain tables, so anon key has full read access.
# Find it: Supabase Dashboard > Project Settings > API > anon public key
key = "YOUR_ANON_KEY_HERE"
